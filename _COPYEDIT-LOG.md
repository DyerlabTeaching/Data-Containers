# Copy-Edit Log — Data-Containers

Tracks copy-editing passes over this module's `.qmd` documents. Clear-cut
typos/grammar/spelling errors are corrected directly in the source. Items
that need an author decision are left in place but wrapped in
`<mark>...</mark>` so they show up highlighted in the rendered HTML.

## narrative.qmd — 2026-08-15

### Fixed directly (typos, spelling, grammar)

| Line | Issue | Fix |
|---|---|---|
| 58 | "a sequnce of values" | "a sequence of values" |
| 60 | "a seqeunce of numerical values" (missing terminal period) | "a sequence of numerical values."|
| 73 | "make a sequences" | "make a sequence" |
| 93 | "we used square brackets" (tense) | "we use square brackets" |
| 107 | "proceeds similarily" | "proceeds similarly" |
| 114 | "fill in the intermediate values wtih" | "with" |
| 144 | "the addition operaiton" | "the addition operation" |
| 158 | "the first vectoer" | "the first vector" |
| 181 | "access indiviudal elements" | "access individual elements" |
| 197 | "the addition of these two matrices require" (agreement) | "requires" |
| 202 | "notic how" | "notice how" |
| 214 | "matrix mutliplication" (×2); "the product or rows" | "matrix multiplication"; "the product of rows" |
| 225 | "sum of the multipliation" | "multiplication" |
| 231 | "a few `character` vluaes" | "values" |
| 254 | "square brakets" | "square brackets" |
| 269 | "use as \$ notation" (stray "as") | "use \$ notation" |
| 272, 304 | `Vegitarian` — misspelled list key used both in the code and in the prose describing it | `Vegetarian` in both places, kept consistent |
| 275 | "Both are equivallent." | "equivalent." |
| 300 | "what is inside of them" (agreement with singular "list") | "what is inside of it" |
| 312 | "if I ran the chunck" | "the chunk" |
| 322 | "return the restuls" | "return the results" |
| 357 | "If fact, the contents" | "In fact, the contents" |
| 366 | "lets just consider"; "We an also easily"; "as in-text r citation" | "let's just consider"; "We can also easily"; "as an in-text r citation" |
| 410 | "may have differnet lengths" | "different lengths" |
| 440 | "here is an example" (sentence-initial lowercase) | "Here is an example" |
| 458 | "the funtion tells us" | "the function tells us" |

### Flagged for your review (highlighted with `<mark>` in the source)

| Line | Text | Why it's flagged |
|---|---|---|
| ~7 | "In this topic, we examine some of the more basic" | The intro blockquote cuts off — "the more basic" what? Container types, presumably, but I didn't want to put words in your mouth. |
